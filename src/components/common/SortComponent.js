import React from "react";

const SortComponent = (props) => {
	return (
		<div className="sort-btn text-center">
			<i className="fa fa-sort-amount-asc"></i>
		</div>
	)
};

export default SortComponent;